# Core dependencies
gradio>=4.0.0
python-dotenv>=1.0.0
cryptography==46.0.5  # Required by api_key_manager for secure key storage

# Image/Video Processing
Pillow>=10.0.0,<12.0  # moviepy compatibility
numpy==1.26.4 # Strict version for all packages
ffmpeg-python>=0.2.0

# AI Models (fixed versions for compatibility)
torch==2.1.0
torchvision==0.16.0
basicsr>=1.4.2
realesrgan>=0.3.0
gfpgan>=1.3.8
facexlib>=0.3.0

# LipSync dependencies
librosa==0.9.2
numba>=0.57.0
llvmlite>=0.40.0
soundfile>=0.12.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
scikit-image>=0.21.0
einops>=0.4.1  # Per Video-Retalking transformer models
dlib>=19.24.0  # Per Video-Retalking face detection

# SadTalker specific dependencies
safetensors>=0.4.0
yacs>=0.1.8
pyyaml>=6.0
kornia>=0.6.8
av>=10.0.0
scipy>=1.10.0
face-alignment>=1.3.5

# LivePortrait specific dependencies
onnxruntime>=1.16.0  # Per inferenza modelli ONNX
tyro>=0.5.0  # Per parsing argomenti CLI

# Utilities
tqdm>=4.66.0
requests>=2.31.0
huggingface-hub[cli]>=0.20.0  # For downloading LivePortrait checkpoints

# Testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0

# Subtitles generation
faster-whisper>=1.0.0  # Faster than openai-whisper, less VRAM
openai>=1.0.0  # For GPT API and model listing
pytest-mock>=3.11.0

# AI Background Removal
# Note: rembg has some dependency conflicts that are auto-fixed at setup/startup
# The app applies a compatibility patch automatically (see utils/dependency_patch.py)
rembg # AI background removal
pymatting==1.1.15  # Required by rembg

# AI Image to Video
diffusers==0.30.3  # Stable Video Diffusion
accelerate==0.34.2  # Required for diffusers optimization
transformers==4.44.2  # Compatible with diffusers 0.30.3 (has FLAX_WEIGHTS_NAME)
soxr>=0.3.0  # Required by transformers for audio processing
